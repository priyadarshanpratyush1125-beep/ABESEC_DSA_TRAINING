count(s.begin(),s.end(),'target')  if target is in the string then give 1 otherwise 0

basic operation stl c++
accumulate(arr,arr+length,inital_sum);

min_element(arr,arr+length);
max_element(arr,arr+length);
sort(arr_name,arr_name+length ,comp_function);
in asc ----sort(arr_name,arr_name+length,less<int>());
in dec-----sort(arr_name,arr_name+length,greater<int>);


sort function is implace in c++


access and update element 
a[i]=9

int index=find(arr,arr+len,element)------>gives address of element so to access element we use A[index]=element
note if element is not found then it return size of array

array equality 
bool ans=equal(arr1,arr1+len,arr2);

reverse array
reverse(arr,arr+len);---->in place 

printing arrary ----->
method 1 for(int i=0;i<num;i++){
   cout<<A[i];}
method -2

for(int num: arr_name){
cout<<num<<endl;}





2.dynamic array/vector
vector<datatype> vector_name;   here all value of each block of vector is 0   but in array each block size id garbage value if we print V[1] it gives 0 and in A[1] it gives 6549646464 

vector<data_type>vectore_name(size,defultvalue);
ex--vector<int>v(3,10);          vector<int>(3,0)==vector<int>(3)
cout------>10 ,10, 10             cout-----0,0,0

vector<int>v={1,2,3,4,5,6};


swap(vector1[1],vector2[1]);




